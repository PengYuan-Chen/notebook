<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/create.css">
</head>
<body>
    <div class="container">
        <h2 id="header">修改</h2>
        <hr>
        <form action="/change/<%= data[0]._id %>" method="post">
            事項<input type="text" name="title" value="<%= data[0].title %>">
            時間<input type="date" name="time" value="<%= moment(data[0].time).format('YYYY-MM-DD') %>">
            類型<select name="type">
                <% if(data[0].type =='-1'){ %>
                    <option value="-1" selected>支出</option>
                    <option value="1">收入</option>
                <% }else{ %>
                    <option value="-1" >支出</option>
                    <option value="1" selected>收入</option>
                <% } %>
                </select>
            金額<input type="number" name="account" value="<%=data[0].account %>">
            備註<input type="text" name="remarks" value="<%= data[0].remarks %>">
            <button>提交</button>
        </form>
    </div>
</body>
</html>